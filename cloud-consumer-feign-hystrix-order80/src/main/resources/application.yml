server:
  port: 80

eureka:
  client:
    register-with-eureka: false #不用注册到eureka中
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka,http://eureka7002.com:7002/eureka
feign:
  hystrix:
    enabled: true
